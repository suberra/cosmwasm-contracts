# `suberra-core`

This is a collection of common types and querier which as commonly used in Suberra contracts. `suberra-core` package handles the common interfaces in the contracts, such as AccountCreationHooks and other `InstantiateMsg` structs


